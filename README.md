This script allows to to fetch result from semi de paris as excel file

```bash
# get results 
$ python .\featch-semi-paris.py <filename>

# get results + step's speeds and ranking
$ python .\featch-semi-paris.py deep <filename>
```
